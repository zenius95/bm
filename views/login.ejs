<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-900 text-gray-50 flex items-center justify-center min-h-screen">
    <div class="fixed inset-0 -z-10 h-full w-full">
        <div class="absolute top-0 left-0 h-96 w-96 bg-purple-500/40 rounded-full blur-[150px] opacity-30"></div>
        <div class="absolute bottom-0 right-0 h-80 w-80 bg-blue-500/40 rounded-full blur-[120px] opacity-40"></div>
    </div>

    <div class="w-full max-w-md p-8 space-y-8 bg-gray-800/50 backdrop-blur-lg border border-white/10 rounded-2xl shadow-2xl">
        <div>
            <h1 class="text-3xl font-bold text-center text-white">ADMINPRO</h1>
            <p class="mt-2 text-center text-sm text-gray-400">Đăng nhập vào tài khoản của Bro</p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="bg-red-500/20 border border-red-500/30 text-red-300 px-4 py-3 rounded-lg text-sm">
                <%= error %>
            </div>
        <% } %>

        <form class="space-y-6" action="/login" method="POST">
            <div>
                <label for="username" class="block text-sm font-medium text-gray-300">Username</label>
                <div class="mt-2">
                    <input id="username" name="username" type="text" required class="w-full bg-gray-900/50 border border-white/20 rounded-lg p-3 text-sm text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200">
                </div>
            </div>
            <div>
                <label for="password" class="block text-sm font-medium text-gray-300">Password</label>
                <div class="mt-2">
                    <input id="password" name="password" type="password" required class="w-full bg-gray-900/50 border border-white/20 rounded-lg p-3 text-sm text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200">
                </div>
            </div>
            <div>
                <button type="submit" class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-transform duration-200 hover:scale-105">
                    Đăng Nhập
                </button>
            </div>
        </form>
    </div>
</body>
</html>