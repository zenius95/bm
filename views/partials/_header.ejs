<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <style>
        :root { 
            --sidebar-width: 260px;
            --bs-body-bg: #16161a; 
            --bs-dark-rgb: 36, 36, 40;
            --bs-border-color: rgba(255, 255, 255, 0.1);
        }
        body { display: flex; min-height: 100vh; overflow-x: hidden; background-color: var(--bs-body-bg); color: #efeff1; }
        .sidebar { width: var(--sidebar-width); flex-shrink: 0; background-color: #1c1c21; }
        .main-content { flex-grow: 1; padding: 2.5rem; }
        .sidebar .nav-link { font-size: 1rem; color: #a7a9be; border-radius: 0.5rem; transition: all 0.2s ease-in-out; }
        .sidebar .nav-link.active { background-color: var(--bs-primary); color: var(--bs-white); }
        .sidebar .nav-link:not(.active):hover { background-color: var(--bs-dark); color: var(--bs-light); }
        .card { background-color: #242428; border-color: var(--bs-border-color); }
        .table { --bs-table-bg: #242428; --bs-table-striped-bg: #2a2a2e; }
        .table-hover tbody tr:hover { background-color: #313135; }
        .empty-state { padding: 4rem 2rem; text-align: center; border: 2px dashed var(--bs-border-color); border-radius: .5rem; }
        .empty-state .icon { font-size: 4rem; color: var(--bs-secondary); }

        .toast-container { position: fixed; top: 1.5rem; right: 1.5rem; z-index: 1090; }

        /* === START: THAY ĐỔI QUAN TRỌNG === */
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .icon-spin {
            animation: spin 1s linear infinite;
        }
        /* === END: THAY ĐỔI QUAN TRỌNG === */
    </style>
</head>
<body>
    <%- include('_sidebar.ejs', { page: typeof page !== 'undefined' ? page : '' }) %>