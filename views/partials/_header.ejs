<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
    

            extend: {
                    fontFamily: { sans: ['Inter', 'sans-serif'] },
                    colors: {
                        'gray': {
                

            950: '#09090b', 900: '#18181b', 800: '#27272a', 
                            700: '#3f3f46', 200: '#e4e4e7', 50: '#fafafa',   
                        },
                    }
  

              }
            },
            /* === START: THAY ĐỔI QUAN TRỌNG === */
            plugins: [
                function({ addComponents }) {
                    addComponents({
                        '.form-input': {
                            '@apply w-full bg-gray-900/50 border border-white/20 rounded-lg p-3 text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200 shadow-sm': {},
                        },
                        '.form-select': {
                            '@apply w-full bg-gray-900/50 border border-white/20 rounded-lg pl-3 pr-10 py-3 text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200 shadow-sm appearance-none': {},
                            'backgroundImage': `url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e")`,
                            'backgroundPosition': 'right 0.5rem center',
                            'backgroundRepeat': 'no-repeat',
                            'backgroundSize': '1.5em 1.5em',
                        }
                    })
                }
            ]
            /* === END: THAY ĐỔI QUAN TRỌNG === */
        }
    </script>

    <style>
        body { -webkit-font-smoothing: antialiased;
-moz-osx-font-smoothing: grayscale; }
        ::-webkit-scrollbar { width: 8px; height: 8px;
}
        ::-webkit-scrollbar-track { background: #18181b;
}
        ::-webkit-scrollbar-thumb { background: #3f3f46; border-radius: 10px;
}
        ::-webkit-scrollbar-thumb:hover { background: #52525b;
}
        @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg);
} }
        .icon-spin { animation: spin 1s linear infinite;
}
    </style>
</head>
<body class="bg-gray-950 text-gray-50 font-sans">
    
    <div class="fixed inset-0 -z-10 h-full w-full">
        <div class="absolute top-0 left-0 h-96 w-96 bg-purple-500/40 rounded-full blur-[150px] opacity-30"></div>
        <div class="absolute bottom-0 right-0 h-80 w-80 bg-blue-500/40 rounded-full blur-[120px] opacity-40"></div>
        <div class="absolute bottom-1/4 left-1/4 h-72 w-72 bg-pink-500/30 rounded-full blur-[100px] opacity-30"></div>
    </div>
    
    <div class="flex min-h-screen w-full relative z-10">
       

 <%- include('_sidebar.ejs', { page: typeof page !== 'undefined' ? page : '' }) %>
        
        <div class="flex-1 flex flex-col">
            <main class="flex-grow p-6 sm:p-8">